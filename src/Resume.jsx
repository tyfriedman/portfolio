import React from 'react';
import './Resume.css'; // Optionally, create a separate CSS file for Resume page styling

const Resume = () => {
  return (
    <div className="resume-page">
      <h1>My Resume</h1>
      {/* Add your resume content here */}
    </div>
  );
};

export default Resume;
