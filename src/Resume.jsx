import React from 'react';
import './Resume.css'; // Create a separate CSS file for styling the resume page if needed

const Resume = () => {
  return (
    <div className="resume-container">
      <h1>My Resume</h1>
      {/* Add your resume content here */}
    </div>
  );
};

export default Resume;
